using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BlockingBlock : MonoBehaviour //Eduardo --> Classe criada para coletar informacoes do bloco que atrapalha a passagem do player
{
    // Start is called before the first frame update
     [HideInInspector] public TileLogic tile;// Onde a unidade consegue identificar onde está.
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
